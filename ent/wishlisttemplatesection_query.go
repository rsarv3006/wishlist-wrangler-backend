// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"wishlist-wrangler-api/ent/predicate"
	"wishlist-wrangler-api/ent/wishlisttemplatesection"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// WishlistTemplateSectionQuery is the builder for querying WishlistTemplateSection entities.
type WishlistTemplateSectionQuery struct {
	config
	ctx        *QueryContext
	order      []wishlisttemplatesection.OrderOption
	inters     []Interceptor
	predicates []predicate.WishlistTemplateSection
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WishlistTemplateSectionQuery builder.
func (wtsq *WishlistTemplateSectionQuery) Where(ps ...predicate.WishlistTemplateSection) *WishlistTemplateSectionQuery {
	wtsq.predicates = append(wtsq.predicates, ps...)
	return wtsq
}

// Limit the number of records to be returned by this query.
func (wtsq *WishlistTemplateSectionQuery) Limit(limit int) *WishlistTemplateSectionQuery {
	wtsq.ctx.Limit = &limit
	return wtsq
}

// Offset to start from.
func (wtsq *WishlistTemplateSectionQuery) Offset(offset int) *WishlistTemplateSectionQuery {
	wtsq.ctx.Offset = &offset
	return wtsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wtsq *WishlistTemplateSectionQuery) Unique(unique bool) *WishlistTemplateSectionQuery {
	wtsq.ctx.Unique = &unique
	return wtsq
}

// Order specifies how the records should be ordered.
func (wtsq *WishlistTemplateSectionQuery) Order(o ...wishlisttemplatesection.OrderOption) *WishlistTemplateSectionQuery {
	wtsq.order = append(wtsq.order, o...)
	return wtsq
}

// First returns the first WishlistTemplateSection entity from the query.
// Returns a *NotFoundError when no WishlistTemplateSection was found.
func (wtsq *WishlistTemplateSectionQuery) First(ctx context.Context) (*WishlistTemplateSection, error) {
	nodes, err := wtsq.Limit(1).All(setContextOp(ctx, wtsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wishlisttemplatesection.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) FirstX(ctx context.Context) *WishlistTemplateSection {
	node, err := wtsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WishlistTemplateSection ID from the query.
// Returns a *NotFoundError when no WishlistTemplateSection ID was found.
func (wtsq *WishlistTemplateSectionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = wtsq.Limit(1).IDs(setContextOp(ctx, wtsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wishlisttemplatesection.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := wtsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WishlistTemplateSection entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WishlistTemplateSection entity is found.
// Returns a *NotFoundError when no WishlistTemplateSection entities are found.
func (wtsq *WishlistTemplateSectionQuery) Only(ctx context.Context) (*WishlistTemplateSection, error) {
	nodes, err := wtsq.Limit(2).All(setContextOp(ctx, wtsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wishlisttemplatesection.Label}
	default:
		return nil, &NotSingularError{wishlisttemplatesection.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) OnlyX(ctx context.Context) *WishlistTemplateSection {
	node, err := wtsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WishlistTemplateSection ID in the query.
// Returns a *NotSingularError when more than one WishlistTemplateSection ID is found.
// Returns a *NotFoundError when no entities are found.
func (wtsq *WishlistTemplateSectionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = wtsq.Limit(2).IDs(setContextOp(ctx, wtsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wishlisttemplatesection.Label}
	default:
		err = &NotSingularError{wishlisttemplatesection.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := wtsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WishlistTemplateSections.
func (wtsq *WishlistTemplateSectionQuery) All(ctx context.Context) ([]*WishlistTemplateSection, error) {
	ctx = setContextOp(ctx, wtsq.ctx, ent.OpQueryAll)
	if err := wtsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WishlistTemplateSection, *WishlistTemplateSectionQuery]()
	return withInterceptors[[]*WishlistTemplateSection](ctx, wtsq, qr, wtsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) AllX(ctx context.Context) []*WishlistTemplateSection {
	nodes, err := wtsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WishlistTemplateSection IDs.
func (wtsq *WishlistTemplateSectionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if wtsq.ctx.Unique == nil && wtsq.path != nil {
		wtsq.Unique(true)
	}
	ctx = setContextOp(ctx, wtsq.ctx, ent.OpQueryIDs)
	if err = wtsq.Select(wishlisttemplatesection.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := wtsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wtsq *WishlistTemplateSectionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wtsq.ctx, ent.OpQueryCount)
	if err := wtsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wtsq, querierCount[*WishlistTemplateSectionQuery](), wtsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) CountX(ctx context.Context) int {
	count, err := wtsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wtsq *WishlistTemplateSectionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wtsq.ctx, ent.OpQueryExist)
	switch _, err := wtsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wtsq *WishlistTemplateSectionQuery) ExistX(ctx context.Context) bool {
	exist, err := wtsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WishlistTemplateSectionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wtsq *WishlistTemplateSectionQuery) Clone() *WishlistTemplateSectionQuery {
	if wtsq == nil {
		return nil
	}
	return &WishlistTemplateSectionQuery{
		config:     wtsq.config,
		ctx:        wtsq.ctx.Clone(),
		order:      append([]wishlisttemplatesection.OrderOption{}, wtsq.order...),
		inters:     append([]Interceptor{}, wtsq.inters...),
		predicates: append([]predicate.WishlistTemplateSection{}, wtsq.predicates...),
		// clone intermediate query.
		sql:  wtsq.sql.Clone(),
		path: wtsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WishlistTemplateSection.Query().
//		GroupBy(wishlisttemplatesection.FieldTitle).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wtsq *WishlistTemplateSectionQuery) GroupBy(field string, fields ...string) *WishlistTemplateSectionGroupBy {
	wtsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WishlistTemplateSectionGroupBy{build: wtsq}
	grbuild.flds = &wtsq.ctx.Fields
	grbuild.label = wishlisttemplatesection.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Title string `json:"title,omitempty"`
//	}
//
//	client.WishlistTemplateSection.Query().
//		Select(wishlisttemplatesection.FieldTitle).
//		Scan(ctx, &v)
func (wtsq *WishlistTemplateSectionQuery) Select(fields ...string) *WishlistTemplateSectionSelect {
	wtsq.ctx.Fields = append(wtsq.ctx.Fields, fields...)
	sbuild := &WishlistTemplateSectionSelect{WishlistTemplateSectionQuery: wtsq}
	sbuild.label = wishlisttemplatesection.Label
	sbuild.flds, sbuild.scan = &wtsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WishlistTemplateSectionSelect configured with the given aggregations.
func (wtsq *WishlistTemplateSectionQuery) Aggregate(fns ...AggregateFunc) *WishlistTemplateSectionSelect {
	return wtsq.Select().Aggregate(fns...)
}

func (wtsq *WishlistTemplateSectionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wtsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wtsq); err != nil {
				return err
			}
		}
	}
	for _, f := range wtsq.ctx.Fields {
		if !wishlisttemplatesection.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wtsq.path != nil {
		prev, err := wtsq.path(ctx)
		if err != nil {
			return err
		}
		wtsq.sql = prev
	}
	return nil
}

func (wtsq *WishlistTemplateSectionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WishlistTemplateSection, error) {
	var (
		nodes = []*WishlistTemplateSection{}
		_spec = wtsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WishlistTemplateSection).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WishlistTemplateSection{config: wtsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wtsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wtsq *WishlistTemplateSectionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wtsq.querySpec()
	_spec.Node.Columns = wtsq.ctx.Fields
	if len(wtsq.ctx.Fields) > 0 {
		_spec.Unique = wtsq.ctx.Unique != nil && *wtsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wtsq.driver, _spec)
}

func (wtsq *WishlistTemplateSectionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wishlisttemplatesection.Table, wishlisttemplatesection.Columns, sqlgraph.NewFieldSpec(wishlisttemplatesection.FieldID, field.TypeUUID))
	_spec.From = wtsq.sql
	if unique := wtsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wtsq.path != nil {
		_spec.Unique = true
	}
	if fields := wtsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wishlisttemplatesection.FieldID)
		for i := range fields {
			if fields[i] != wishlisttemplatesection.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wtsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wtsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wtsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wtsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wtsq *WishlistTemplateSectionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wtsq.driver.Dialect())
	t1 := builder.Table(wishlisttemplatesection.Table)
	columns := wtsq.ctx.Fields
	if len(columns) == 0 {
		columns = wishlisttemplatesection.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wtsq.sql != nil {
		selector = wtsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wtsq.ctx.Unique != nil && *wtsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wtsq.predicates {
		p(selector)
	}
	for _, p := range wtsq.order {
		p(selector)
	}
	if offset := wtsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wtsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WishlistTemplateSectionGroupBy is the group-by builder for WishlistTemplateSection entities.
type WishlistTemplateSectionGroupBy struct {
	selector
	build *WishlistTemplateSectionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wtsgb *WishlistTemplateSectionGroupBy) Aggregate(fns ...AggregateFunc) *WishlistTemplateSectionGroupBy {
	wtsgb.fns = append(wtsgb.fns, fns...)
	return wtsgb
}

// Scan applies the selector query and scans the result into the given value.
func (wtsgb *WishlistTemplateSectionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wtsgb.build.ctx, ent.OpQueryGroupBy)
	if err := wtsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WishlistTemplateSectionQuery, *WishlistTemplateSectionGroupBy](ctx, wtsgb.build, wtsgb, wtsgb.build.inters, v)
}

func (wtsgb *WishlistTemplateSectionGroupBy) sqlScan(ctx context.Context, root *WishlistTemplateSectionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wtsgb.fns))
	for _, fn := range wtsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wtsgb.flds)+len(wtsgb.fns))
		for _, f := range *wtsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wtsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wtsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WishlistTemplateSectionSelect is the builder for selecting fields of WishlistTemplateSection entities.
type WishlistTemplateSectionSelect struct {
	*WishlistTemplateSectionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wtss *WishlistTemplateSectionSelect) Aggregate(fns ...AggregateFunc) *WishlistTemplateSectionSelect {
	wtss.fns = append(wtss.fns, fns...)
	return wtss
}

// Scan applies the selector query and scans the result into the given value.
func (wtss *WishlistTemplateSectionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wtss.ctx, ent.OpQuerySelect)
	if err := wtss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WishlistTemplateSectionQuery, *WishlistTemplateSectionSelect](ctx, wtss.WishlistTemplateSectionQuery, wtss, wtss.inters, v)
}

func (wtss *WishlistTemplateSectionSelect) sqlScan(ctx context.Context, root *WishlistTemplateSectionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wtss.fns))
	for _, fn := range wtss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wtss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wtss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
