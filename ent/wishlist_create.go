// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"
	"wishlist-wrangler-api/ent/user"
	"wishlist-wrangler-api/ent/wishlist"
	"wishlist-wrangler-api/ent/wishlistsection"
	"wishlist-wrangler-api/ent/wishlisttemplate"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// WishlistCreate is the builder for creating a Wishlist entity.
type WishlistCreate struct {
	config
	mutation *WishlistMutation
	hooks    []Hook
}

// SetTitle sets the "title" field.
func (wc *WishlistCreate) SetTitle(s string) *WishlistCreate {
	wc.mutation.SetTitle(s)
	return wc
}

// SetCreatedAt sets the "created_at" field.
func (wc *WishlistCreate) SetCreatedAt(t time.Time) *WishlistCreate {
	wc.mutation.SetCreatedAt(t)
	return wc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (wc *WishlistCreate) SetNillableCreatedAt(t *time.Time) *WishlistCreate {
	if t != nil {
		wc.SetCreatedAt(*t)
	}
	return wc
}

// SetStatus sets the "status" field.
func (wc *WishlistCreate) SetStatus(w wishlist.Status) *WishlistCreate {
	wc.mutation.SetStatus(w)
	return wc
}

// SetNillableStatus sets the "status" field if the given value is not nil.
func (wc *WishlistCreate) SetNillableStatus(w *wishlist.Status) *WishlistCreate {
	if w != nil {
		wc.SetStatus(*w)
	}
	return wc
}

// SetID sets the "id" field.
func (wc *WishlistCreate) SetID(u uuid.UUID) *WishlistCreate {
	wc.mutation.SetID(u)
	return wc
}

// SetNillableID sets the "id" field if the given value is not nil.
func (wc *WishlistCreate) SetNillableID(u *uuid.UUID) *WishlistCreate {
	if u != nil {
		wc.SetID(*u)
	}
	return wc
}

// AddCreatorIDs adds the "creator" edge to the User entity by IDs.
func (wc *WishlistCreate) AddCreatorIDs(ids ...uuid.UUID) *WishlistCreate {
	wc.mutation.AddCreatorIDs(ids...)
	return wc
}

// AddCreator adds the "creator" edges to the User entity.
func (wc *WishlistCreate) AddCreator(u ...*User) *WishlistCreate {
	ids := make([]uuid.UUID, len(u))
	for i := range u {
		ids[i] = u[i].ID
	}
	return wc.AddCreatorIDs(ids...)
}

// AddTemplateIDs adds the "template" edge to the WishlistTemplate entity by IDs.
func (wc *WishlistCreate) AddTemplateIDs(ids ...uuid.UUID) *WishlistCreate {
	wc.mutation.AddTemplateIDs(ids...)
	return wc
}

// AddTemplate adds the "template" edges to the WishlistTemplate entity.
func (wc *WishlistCreate) AddTemplate(w ...*WishlistTemplate) *WishlistCreate {
	ids := make([]uuid.UUID, len(w))
	for i := range w {
		ids[i] = w[i].ID
	}
	return wc.AddTemplateIDs(ids...)
}

// AddSectionIDs adds the "sections" edge to the WishlistSection entity by IDs.
func (wc *WishlistCreate) AddSectionIDs(ids ...uuid.UUID) *WishlistCreate {
	wc.mutation.AddSectionIDs(ids...)
	return wc
}

// AddSections adds the "sections" edges to the WishlistSection entity.
func (wc *WishlistCreate) AddSections(w ...*WishlistSection) *WishlistCreate {
	ids := make([]uuid.UUID, len(w))
	for i := range w {
		ids[i] = w[i].ID
	}
	return wc.AddSectionIDs(ids...)
}

// Mutation returns the WishlistMutation object of the builder.
func (wc *WishlistCreate) Mutation() *WishlistMutation {
	return wc.mutation
}

// Save creates the Wishlist in the database.
func (wc *WishlistCreate) Save(ctx context.Context) (*Wishlist, error) {
	wc.defaults()
	return withHooks(ctx, wc.sqlSave, wc.mutation, wc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (wc *WishlistCreate) SaveX(ctx context.Context) *Wishlist {
	v, err := wc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wc *WishlistCreate) Exec(ctx context.Context) error {
	_, err := wc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wc *WishlistCreate) ExecX(ctx context.Context) {
	if err := wc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (wc *WishlistCreate) defaults() {
	if _, ok := wc.mutation.CreatedAt(); !ok {
		v := wishlist.DefaultCreatedAt()
		wc.mutation.SetCreatedAt(v)
	}
	if _, ok := wc.mutation.Status(); !ok {
		v := wishlist.DefaultStatus
		wc.mutation.SetStatus(v)
	}
	if _, ok := wc.mutation.ID(); !ok {
		v := wishlist.DefaultID()
		wc.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wc *WishlistCreate) check() error {
	if _, ok := wc.mutation.Title(); !ok {
		return &ValidationError{Name: "title", err: errors.New(`ent: missing required field "Wishlist.title"`)}
	}
	if v, ok := wc.mutation.Title(); ok {
		if err := wishlist.TitleValidator(v); err != nil {
			return &ValidationError{Name: "title", err: fmt.Errorf(`ent: validator failed for field "Wishlist.title": %w`, err)}
		}
	}
	if _, ok := wc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "Wishlist.created_at"`)}
	}
	if _, ok := wc.mutation.Status(); !ok {
		return &ValidationError{Name: "status", err: errors.New(`ent: missing required field "Wishlist.status"`)}
	}
	if v, ok := wc.mutation.Status(); ok {
		if err := wishlist.StatusValidator(v); err != nil {
			return &ValidationError{Name: "status", err: fmt.Errorf(`ent: validator failed for field "Wishlist.status": %w`, err)}
		}
	}
	return nil
}

func (wc *WishlistCreate) sqlSave(ctx context.Context) (*Wishlist, error) {
	if err := wc.check(); err != nil {
		return nil, err
	}
	_node, _spec := wc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(*uuid.UUID); ok {
			_node.ID = *id
		} else if err := _node.ID.Scan(_spec.ID.Value); err != nil {
			return nil, err
		}
	}
	wc.mutation.id = &_node.ID
	wc.mutation.done = true
	return _node, nil
}

func (wc *WishlistCreate) createSpec() (*Wishlist, *sqlgraph.CreateSpec) {
	var (
		_node = &Wishlist{config: wc.config}
		_spec = sqlgraph.NewCreateSpec(wishlist.Table, sqlgraph.NewFieldSpec(wishlist.FieldID, field.TypeUUID))
	)
	if id, ok := wc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = &id
	}
	if value, ok := wc.mutation.Title(); ok {
		_spec.SetField(wishlist.FieldTitle, field.TypeString, value)
		_node.Title = value
	}
	if value, ok := wc.mutation.CreatedAt(); ok {
		_spec.SetField(wishlist.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if value, ok := wc.mutation.Status(); ok {
		_spec.SetField(wishlist.FieldStatus, field.TypeEnum, value)
		_node.Status = value
	}
	if nodes := wc.mutation.CreatorIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   wishlist.CreatorTable,
			Columns: []string{wishlist.CreatorColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(user.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := wc.mutation.TemplateIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   wishlist.TemplateTable,
			Columns: []string{wishlist.TemplateColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(wishlisttemplate.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := wc.mutation.SectionsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   wishlist.SectionsTable,
			Columns: []string{wishlist.SectionsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(wishlistsection.FieldID, field.TypeUUID),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// WishlistCreateBulk is the builder for creating many Wishlist entities in bulk.
type WishlistCreateBulk struct {
	config
	err      error
	builders []*WishlistCreate
}

// Save creates the Wishlist entities in the database.
func (wcb *WishlistCreateBulk) Save(ctx context.Context) ([]*Wishlist, error) {
	if wcb.err != nil {
		return nil, wcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(wcb.builders))
	nodes := make([]*Wishlist, len(wcb.builders))
	mutators := make([]Mutator, len(wcb.builders))
	for i := range wcb.builders {
		func(i int, root context.Context) {
			builder := wcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WishlistMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (wcb *WishlistCreateBulk) SaveX(ctx context.Context) []*Wishlist {
	v, err := wcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (wcb *WishlistCreateBulk) Exec(ctx context.Context) error {
	_, err := wcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (wcb *WishlistCreateBulk) ExecX(ctx context.Context) {
	if err := wcb.Exec(ctx); err != nil {
		panic(err)
	}
}
